test_that("query functions work", {
  # con <- db_connect()
  # 
  # ### telemetry
  # db_query_deployment_period(con, collect = FALSE)
  # db_query_capture_tidy(con, collect = FALSE)
  # db_query_detection_clean(con, collect = FALSE)
  # db_query_detection_station(con, collect = FALSE)
  # db_query_detection_tidy(con, collect = FALSE)
  # db_query_detection_tidy(con, clean = FALSE, collect = FALSE)
  # db_query_detection_timestep(con, collect = FALSE)
  # db_query_detection_timestep(con, timestep = "day", collect = FALSE)
  # db_query_detection_timestep(con, clean = FALSE, collect = FALSE)
  # 
  # ### environmental
  # db_query_temperature_deployment_period(con, collect = FALSE)
  # db_query_temperature_clean(con, collect = FALSE)
  # db_query_temperature_tidy(con, collect = FALSE)
  
})
